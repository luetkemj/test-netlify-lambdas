[build]
  command = "npm run build" # your build command
  publish = "./deploy/dist" # where your production ready client code lives AFTER your build command has run
  functions = "./deploy/functions" # where your production ready lambda functions live AFTER your build command has run
